# 项目架构说明

## 📋 目录

- [整体架构](#整体架构)
- [目录结构](#目录结构)
- [核心模块](#核心模块)
- [数据流](#数据流)
- [设计模式](#设计模式)
- [技术栈](#技术栈)
- [文档导航](#文档导航)

---

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户界面层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ run_benchmark│  │ single_test  │  │ verify_...   │      │
│  │    .py       │  │    .py       │  │    .py       │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
└─────────┼──────────────────┼──────────────────┼─────────────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      配置和工具层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │config.yaml   │  │ config_      │  │ error_       │      │
│  │              │  │ loader.py    │  │ messages.py  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      测试执行层                               │
│  ┌──────────────────────────────────────────────────┐       │
│  │         BenchmarkRunner (协调器)                  │       │
│  │  - 初始化客户端                                   │       │
│  │  - 执行测试                                       │       │
│  │  - 收集指标                                       │       │
│  └──────────────────────────────────────────────────┘       │
│                              │                              │
│         ┌────────────────────┼────────────────────┐         │
│         ▼                    ▼                    ▼         │
│  ┌──────────┐        ┌──────────┐        ┌──────────┐    │
│  │Metrics   │        │Test      │        │Judge     │    │
│  │Collector │        │Result    │        │Manager   │    │
│  └──────────┘        └──────────┘        └──────────┘    │
└─────────────────────────────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      API 客户端层                             │
│  ┌──────────────────────────────────────────────────┐       │
│  │              BaseClient (抽象基类)                │       │
│  └──────────────────────────────────────────────────┘       │
│                              ▲                              │
│          ┌───────────────────┼───────────────────┐          │
│          │                   │                   │          │
│  ┌───────┴───────┐  ┌───────┴───────┐  ┌───────┴───────┐ │
│  │StreamingMixin │  │               │  │               │ │
│  │(混入类)       │  │               │  │               │ │
│  └───────┬───────┘  │               │  │               │ │
│          │           │               │  │               │ │
│  ┌───────┴───────────┴───────┐  │   │  ┌──────────────┴─┴┐
│  │  OpenAIClient      │  │   │  │  │AnthropicClient│  │
│  │(OpenAI 协议)      │  │   │  │  │(Anthropic 协议)│  │
│  │- DeepSeek         │  │   │  │  │- GLM           │  │
│  └───────────────────┘  │   │  │  │- MiniMax       │  │
│                          │   │  │  └───────────────────┘  │
│  ┌───────────────────────┴───┴──┴───────────────────────┐  │
│  │              MiniMaxClient (专用)                    │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      测试用例层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │test_registry │  │minimax_      │  │ cases/       │      │
│  │.py           │  │registry.py   │  │cases_minimax/│      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      报告生成层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │Markdown      │  │MiniMax       │  │JSON Saver    │      │
│  │Generator     │  │Generator     │  │              │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 目录结构

```
ds_glm_compare/
│
├── 📄 核心脚本
│   ├── run_benchmark.py        # 主入口 - 支持 4 种运行模式
│   ├── single_test.py          # 单用例测试工具
│   └── verify_config.py        # 配置验证工具
│
├── 📚 文档
│   ├── README.md               # 项目说明
│   ├── CLAUDE.md               # 开发指南（Claude Code）
│   ├── QUICKSTART.md           # 快速开始
│   ├── PROJECT_STRUCTURE.md    # 项目结构详解
│   └── docs/                   # 核心文档（新增）
│       ├── api_architecture.md          # API 架构文档
│       ├── execution_flow.md            # 执行流程文档
│       └── quality_evaluation_system.md # 质量评价体系
│
├── ⚙️ 配置
│   ├── config.yaml             # 主配置文件
│   ├── .env                    # 环境变量（API 密钥）
│   └── requirements.txt        # Python 依赖
│
├── 📦 源代码 (src/)
│   │
│   ├── 🌐 api/                 # API 客户端层
│   │   ├── base_client.py              # 抽象基类
│   │   ├── streaming_mixin.py          # 流式处理混入类 ⭐新增
│   │   ├── constants.py                # 常量配置 ⭐新增
│   │   ├── openai_client.py            # OpenAI 协议
│   │   ├── anthropic_client.py         # Anthropic 协议
│   │   └── minimax_client.py           # MiniMax 专用
│   │
│   ├── 🏃 benchmark/           # 基准测试引擎
│   │   ├── runner.py                    # 测试执行器
│   │   ├── metrics_collector.py         # 指标收集器
│   │   └── test_result.py               # 测试结果模型
│   │
│   ├── 🧪 tests/                # 测试用例
│   │   ├── base_test.py                 # 测试基类
│   │   ├── test_registry.py             # 标准测试注册表
│   │   ├── minimax_registry.py          # MiniMax 测试注册表
│   │   ├── cases/                       # 标准测试用例（111个）
│   │   │   ├── qa_simple.py
│   │   │   ├── code_generation.py
│   │   │   ├── reasoning_complex.py
│   │   │   └── ...
│   │   └── cases_minimax/               # MiniMax 测试用例（100个）
│   │       ├── basic_performance.py
│   │       ├── core_capabilities.py
│   │       ├── practical_scenarios.py
│   │       └── advanced_features.py
│   │
│   ├── ⚖️ quality/              # 质量评估
│   │   ├── judge_manager.py              # Judge 管理器
│   │   ├── judge_llm.py                  # Judge LLM 实现
│   │   ├── scoring_rubric.py             # 评分标准
│   │   ├── minimax_scorer.py            # MiniMax 评分器
│   │   ├── normalization.py              # 数据标准化
│   │   └── judge_result.py               # 评估结果模型
│   │
│   ├── 📊 report/               # 报告生成
│   │   ├── generator.py                 # 通用报告生成器
│   │   ├── markdown_generator.py         # Markdown 报告
│   │   ├── minimax_generator.py          # MiniMax 报告
│   │   ├── visualizer.py                # 可视化工具
│   │   ├── color_schemes.py             # 配色方案
│   │   └── templates/                   # 报告模板
│   │
│   └── 🔧 utils/                # 工具函数
│       ├── config_loader.py             # 配置加载器
│       ├── logger.py                    # 日志工具
│       ├── json_saver.py                # JSON 保存
│       └── error_messages.py            # 错误消息 ⭐新增
│
├── 📁 数据和结果
│   ├── data/                    # 测试数据
│   └── results/                 # 测试结果
│       ├── data/                # 原始 JSON 数据
│       ├── reports/             # Markdown 报告
│       ├── minimax_reports/     # MiniMax 报告
│       ├── benchmarks/          # 基准测试数据
│       └── quality/             # 质量评估结果
│
└── 📝 其他文档
    ├── MINIMAX_FIX_SUMMARY.md  # MiniMax 修复总结
    └── REFACTORING_SUMMARY.md  # 重构总结
```

---

## 核心模块

### 1. API 客户端层 (`src/api/`)

**职责**：封装不同 LLM API 的访问逻辑，提供统一接口。

**设计模式**：
- **适配器模式**：统一不同 API 的访问方式
- **模板方法模式**：流式处理的通用逻辑

**核心类**：

```
BaseClient (抽象基类)
├── 定义统一接口
├── 计算性能指标
└── 管理模型信息

StreamingMixin (混入类) ⭐新增
├── 提供流式处理通用逻辑
└── 消除代码重复

OpenAIClient
├── 继承 StreamingMixin + BaseClient
├── 实现 OpenAI 协议
└── 用于 DeepSeek 等

AnthropicClient
├── 继承 StreamingMixin + BaseClient
├── 实现 Anthropic 协议
└── 用于 GLM、MiniMax 等

MiniMaxClient
├── 仅继承 BaseClient
├── 专用客户端
└── 用于 MiniMax Judge
```

**关键文件**：
- `base_client.py` - 抽象基类，定义接口和指标计算
- `streaming_mixin.py` ⭐ - 流式处理混入类，消除重复
- `constants.py` ⭐ - 常量配置，集中管理魔法数字
- `openai_client.py` - OpenAI 协议实现
- `anthropic_client.py` - Anthropic 协议实现

### 2. 基准测试引擎 (`src/benchmark/`)

**职责**：协调测试执行，收集性能指标。

**核心类**：

```
BenchmarkRunner (协调器)
├── 初始化 API 客户端
├── 验证连接
├── 执行预热
├── 运行测试
└── 协调质量评估

MetricsCollector (收集器)
├── 收集测试结果
├── 计算统计数据
└── 导出结果数据

TestResult (数据模型)
├── 存储单次测试结果
├── 包含性能指标
└── 包含质量评估
```

**关键文件**：
- `runner.py` - 测试执行器，协调整个测试流程
- `metrics_collector.py` - 指标收集和统计
- `test_result.py` - 测试结果数据模型

### 3. 测试用例层 (`src/tests/`)

**职责**：定义和管理测试用例。

**组织结构**：

```
tests/
├── 标准测试（111个用例）
│   ├── test_registry.py         # 注册表
│   └── cases/                   # 按类别组织
│       ├── qa_simple.py         # 简单问答 (12个)
│       ├── code_generation.py   # 代码生成 (20个)
│       ├── reasoning_complex.py # 复杂推理 (15个)
│       └── ...
│
└── MiniMax 测试（100个用例）
    ├── minimax_registry.py      # 注册表
    └── cases_minimax/           # 按维度组织
        ├── basic_performance.py     # 基础性能 (25个)
        ├── core_capabilities.py     # 核心能力 (25个)
        ├── practical_scenarios.py    # 实用场景 (25个)
        └── advanced_features.py     # 高级特性 (25个)
```

### 4. 质量评估层 (`src/quality/`)

**职责**：使用 Judge LLM 评估模型输出质量。

**架构**：

```
JudgeManager (协调器)
├── 管理多个 Judge
├── 构建评估提示词
├── 协调并行/顺序评估
└── 聚合评估结果

Judge LLMs (裁判)
├── DeepSeek Judge (30%)
├── GLM Judge (30%)
└── MiniMax Judge (40%)

ScoringRubric (评分标准)
├── 10 个类别
├── 各类别有不同的标准
└── 加权计算总分

MiniMaxScoreCalculator
├── 0-10 分制评分
├── Z-score 标准化
└── 统计分析
```

**关键文件**：
- `judge_manager.py` - Judge 管理器（910行）
- `scoring_rubric.py` - 评分标准定义
- `minimax_scorer.py` - MiniMax 评分器

### 5. 报告生成层 (`src/report/`)

**职责**：生成多种格式的测试报告。

**报告类型**：

```
Markdown 报告
├── 静态版本（PNG 图表）
└── 交互版本（Plotly 图表）

HTML 报告
├── MiniMax 标准
└── 原始对比

JSON 数据
├── 原始测试结果
├── 统计摘要
└── 质量评估结果
```

**关键文件**：
- `markdown_generator.py` - Markdown 报告生成器
- `minimax_generator.py` - MiniMax HTML 报告生成器
- `visualizer.py` - 图表生成工具

### 6. 工具函数层 (`src/utils/`)

**职责**：提供通用工具函数。

**关键文件**：
- `config_loader.py` - 配置加载和验证
- `logger.py` - 日志设置
- `json_saver.py` - JSON 数据保存
- `error_messages.py` ⭐ - 统一错误消息

---

## 数据流

### 1. 测试执行流程

```
用户运行 run_benchmark.py
    │
    ├─→ 加载配置 (config.yaml)
    │
    ├─→ 初始化 BenchmarkRunner
    │   │
    │   ├─→ 创建 API 客户端
    │   │   ├─→ OpenAIClient (DeepSeek)
    │   │   └─→ AnthropicClient (GLM)
    │   │
    │   ├─→ 初始化 JudgeManager
    │   │   ├─→ DeepSeek Judge
    │   │   ├─→ GLM Judge
    │   │   └─→ MiniMax Judge
    │   │
    │   └─→ 初始化 MetricsCollector
    │
    ├─→ 验证 API 连接
    │
    ├─→ 预热阶段（每个模型 2 次）
    │
    ├─→ 正式测试
    │   │
    │   └─→ 对每个测试用例：
    │       │
    │       ├─→ 对每个模型：
    │       │   │
    │       │   └─→ 运行 3 次
    │       │       │
    │       │       ├─→ stream_chat()
    │       │       │   │
    │       │       │   ├─→ 记录 TTFT
    │       │       │   ├─→ 记录生成速度
    │       │       │   └─→ 返回 StreamMetrics
    │       │       │
    │       │       ├─→ 创建 TestResult
    │       │       │
    │       │       └─→ 添加到 MetricsCollector
    │       │
    │       └─→ 质量评估（可选）
    │           │
    │           └─→ JudgeManager.evaluate_single_output()
    │               │
    │               ├─→ 构建评估提示词
    │               ├─→ 并行调用多个 Judge
    │               ├─→ 解析评估结果
    │               └─→ 计算加权评分
    │
    ├─→ 数据聚合
    │   │
    │   └─→ MetricsCollector.calculate_category_summary()
    │       ├─→ 均值、中位数、标准差
    │       └─→ 生成 CategorySummary
    │
    └─→ 报告生成
        │
        ├─→ 保存 JSON (json_saver.py)
        ├─→ 生成 Markdown (markdown_generator.py)
        └─→ 生成 HTML (minimax_generator.py)
```

### 2. 性能指标数据流

```
API 响应
    │
    ├─→ 时间戳记录
    │   ├─ request_start
    │   ├─ first_token_time
    │   ├─ token_timestamps[]
    │   └─ request_end
    │
    ├─→ 文本累积
    │   └─ full_text
    │
    └─→ 指标计算 (BaseClient._calculate_metrics())
        │
        ├─→ TTFT = first_token_time - request_start
        ├─→ 总时间 = request_end - request_start
        ├─→ 生成时间 = 总时间 - TTFT
        ├─→ 生成速度 = tokens / 生成时间
        └─→ Token 间延迟 = average(token_timestamps[i] - timestamps[i-1])
```

### 3. 质量评估数据流

```
模型输出
    │
    ├─→ JudgeManager
    │   │
    │   ├─→ 构建提示词
    │   │   ├─ 原始提示词
    │   │   ├─ 模型输出
    │   │   ├─ 评分标准
    │   │   └─ 盲评标记
    │   │
    │   ├─→ 并行调用 Judges
    │   │   ├─→ DeepSeek Judge → 1-5 分
    │   │   ├─→ GLM Judge → 1-5 分
    │   │   └─→ MiniMax Judge → 0-10 分
    │   │
    │   ├─→ 解析结果
    │   │   ├─→ 提取分数
    │   │   ├─→ 提取优缺点
    │   │   └─→ 提取推理
    │   │
    │   └─→ 加权综合
    │       ├─→ 标准化到 0-10 分
    │       ├─→ DeepSeek × 0.30
    │       ├─→ GLM × 0.30
    │       ├─→ MiniMax × 0.40
    │       └─→ 综合得分 = 8.26/10
    │
    └─→ JudgeEvaluation
        ├─→ scores (各标准得分)
        ├─→ overall_score (总分)
        ├─→ strengths (优点)
        ├─→ weaknesses (缺点)
        └─→ reasoning (评估理由)
```

---

## 设计模式

### 1. 适配器模式（Adapter Pattern）

**位置**：`src/api/`

**目的**：统一不同 API 的访问方式

**实现**：
```python
BaseClient (抽象接口)
├── OpenAIClient (适配 OpenAI API)
├── AnthropicClient (适配 Anthropic API)
└── MiniMaxClient (适配 MiniMax API)
```

### 2. 模板方法模式（Template Method Pattern）

**位置**：`src/api/streaming_mixin.py`

**目的**：消除流式处理的代码重复

**实现**：
```python
class StreamingMixin:
    def _execute_streaming_request(self, ...):  # 模板方法
        # 1. 记录开始时间
        # 2. 调用抽象方法 stream_executor()
        # 3. 处理响应
        # 4. 计算指标
```

### 3. 策略模式（Strategy Pattern）

**位置**：`src/quality/scoring_rubric.py`

**目的**：不同测试类别使用不同的评分策略

**实现**：
```python
CATEGORY_RUBRICS = {
    "qa_simple": {...},      # 策略 1
    "code_generation": {...}, # 策略 2
    ...
}
```

### 4. 工厂模式（Factory Pattern）

**位置**：`src/benchmark/runner.py`

**目的**：根据配置创建不同的客户端

**实现**：
```python
def _init_clients(self):
    if api_name == "deepseek":
        client = OpenAIClient(...)
    elif api_name == "glm":
        client = AnthropicClient(...)
```

### 5. 注册表模式（Registry Pattern）

**位置**：`src/tests/test_registry.py`

**目的**：管理所有测试用例

**实现**：
```python
class TestRegistry:
    def register(self, test_case):
        self._tests[test_case.name] = test_case
```

---

## 技术栈

### 核心依赖

```
API 客户端：
├── openai          # OpenAI SDK
├── anthropic       # Anthropic SDK
└── httpx           # 异步 HTTP（MiniMax）

数据处理：
├── numpy           # 数值计算
├── pandas          # 数据分析
└── pyyaml          # 配置解析

可视化：
├── matplotlib      # 静态图表
└── plotly          # 交互式图表

工具库：
├── tenacity        # 重试机制
├── tqdm            # 进度条
├── python-dotenv   # 环境变量
└── colorlog        # 彩色日志
```

### Python 版本

- Python 3.8+

### 关键特性

- **类型注解**：全面使用类型提示
- **异步支持**：httpx 异步请求
- **高精度计时**：time.perf_counter()
- **流式处理**：优先使用流式 API
- **错误恢复**：部分结果返回机制

---

## 扩展性设计

### 添加新的 API 客户端

```python
# 1. 继承基类
class NewAPIClient(StreamingMixin, BaseClient):
    pass

# 2. 实现 stream_chat
def stream_chat(self, messages, **kwargs):
    def stream_executor():
        # 返回迭代器
        yield text_chunk

    return self._execute_streaming_request(...)

# 3. 在 config.yaml 中添加配置
apis:
  new_api:
    base_url: "..."
    api_key_env: "NEW_API_KEY"
```

### 添加新的测试类别

```python
# 1. 在 src/tests/cases/ 中创建新文件
# new_category.py

# 2. 定义测试用例
class NewCategoryTests(BaseTestCategory):
    def get_test_cases(self):
        return [...]
```

### 添加新的 Judge

```python
# 1. 在 config.yaml 中配置
quality:
  judges:
    new_judge:
      type: "openai"
      weight: 0.25

# 2. 在 JudgeManager._init_judges() 中添加初始化逻辑
```

---

## 文档导航

### 核心技术文档

本项目的核心技术文档位于 `docs/` 目录，提供详细的技术实现说明：

1. **[API 架构文档](docs/api_architecture.md)**
   - API 客户端层的设计和实现
   - 类层次结构和设计模式
   - 流式处理流程详解
   - 指标计算方法说明
   - 扩展新客户端指南

2. **[执行流程文档](docs/execution_flow.md)**
   - 完整的测试执行流程（9 个步骤）
   - 每个步骤的详细实现说明
   - 数据流转和配置参数
   - 性能考虑和优化建议
   - 运行示例和输出说明

3. **[质量评价体系文档](docs/quality_evaluation_system.md)**
   - 多模型交叉评价机制
   - 10 个测试类别的评价标准
   - 盲评机制和评分计算
   - MiniMax 标准评分系统
   - 统计分析方法说明

### 项目文档

- **[README.md](README.md)** - 项目概述和快速开始
- **[CLAUDE.md](CLAUDE.md)** - 开发指南和重构历史

---

## 总结

这个项目采用了**分层架构**和**模块化设计**，具有以下特点：

✅ **高内聚低耦合**：每个模块职责清晰
✅ **易于扩展**：添加新功能只需实现接口
✅ **代码复用**：通过混入类消除重复
✅ **完善文档**：三个核心文档详细说明架构
✅ **向后兼容**：重构保持了完全兼容性

核心设计理念：
- **统一接口**：所有 API 客户端提供相同的接口
- **流式优先**：优先使用流式 API 获取精确指标
- **盲评机制**：确保质量评估的客观性
- **统计分析**：全面的统计分析和可视化
